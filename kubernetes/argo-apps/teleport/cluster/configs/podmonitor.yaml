---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: teleport-operator
  namespace: teleport
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: teleport-cluster
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
