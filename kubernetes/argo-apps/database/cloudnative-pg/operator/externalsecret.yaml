---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudnative-pg
spec:
  data:
    - remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: Postgres-Super-User
        metadataPolicy: None
      secretKey: POSTGRES_SUPER_USER
    - remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: Postgres-Super-Password
        metadataPolicy: None
      secretKey: POSTGRES_SUPER_PASS
    - remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: Postgres-Backup-Access-Key-Id
        metadataPolicy: None
      secretKey: POSTGRES_BACKUP_ACCESS_KEY_ID
    - remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: Postgres-Backup-Access-Secret
        metadataPolicy: None
      secretKey: POSTGRES_BACKUP_ACCESS_SECRET
  refreshInterval: "12h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault
  target:
    creationPolicy: Owner
    deletionPolicy: Delete
    name: cloudnative-pg-secret
    template:
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .POSTGRES_SUPER_USER }}"
        password: "{{ .POSTGRES_SUPER_PASS }}"
        backupAccessKeyId: "{{ .POSTGRES_BACKUP_ACCESS_KEY_ID }}"
        backupAccessSecret: "{{ .POSTGRES_BACKUP_ACCESS_SECRET }}"
      engineVersion: v2
      mergePolicy: Replace
