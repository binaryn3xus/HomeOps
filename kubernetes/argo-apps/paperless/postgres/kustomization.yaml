apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: paperless
resources:
  - cluster.yaml
# Temporarily disabled for clean Postgres reset; re-enable after cluster is healthy.
# - objectstore.yaml
# - scheduledbackup.yaml
