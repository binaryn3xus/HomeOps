---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: tailscale-subnet-router-pc
spec:
  tailscale:
    env:
      - name: TS_EXTRA_ARGS
        value: "--snat-subnet-routes=false --stateful-filtering=false"
