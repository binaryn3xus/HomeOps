---
apiVersion: v1
kind: Service
metadata:
  name: pikvm-dns
  annotations:
    external-dns.alpha.kubernetes.io/hostname: pikvm.unscfleet.com
    external-dns.alpha.kubernetes.io/target: 10.0.30.5
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
