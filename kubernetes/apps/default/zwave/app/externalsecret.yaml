---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: zwave
  namespace: default
spec:
  data:
    - remoteRef:
        key: ZWave-Key-S0-Legacy
      secretKey: KEY_S0_Legacy
    - remoteRef:
        key: ZWave-Key-S2-Unauthenticated
      secretKey: KEY_S2_Unauthenticated
    - remoteRef:
        key: ZWave-Key-S2-Authenticated
      secretKey: KEY_S2_Authenticated
    - remoteRef:
        key: ZWave-Key-S2-AccessControl
      secretKey: KEY_S2_AccessControl
    - remoteRef:
        key: ZWave-Key-LR-S2-Authenticated
      secretKey: KEY_LR_S2_Authenticated
    - remoteRef:
        key: ZWave-Key-LR-S2-AccessControl
      secretKey: KEY_LR_S2_AccessControl
  refreshInterval: "12h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault
  target:
    creationPolicy: Owner
    name: zwave-secret
    template:
      data:
        KEY_LR_S2_AccessControl: "{{ .KEY_LR_S2_AccessControl }}"
        KEY_LR_S2_Authenticated: "{{ .KEY_LR_S2_Authenticated }}"
        KEY_S0_Legacy: "{{ .KEY_S0_Legacy }}"
        KEY_S2_AccessControl: "{{ .KEY_S2_AccessControl }}"
        KEY_S2_Authenticated: "{{ .KEY_S2_Authenticated }}"
        KEY_S2_Unauthenticated: "{{ .KEY_S2_Unauthenticated }}"
      engineVersion: v2
