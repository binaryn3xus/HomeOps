apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zigbee
  namespace: default
  annotations:
    gatus.home-operations.com/endpoint: |-
      dns:
        query-name: zigbee.unscfleet.com
        query-type: A
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - zigbee.unscfleet.com
  rules:
    - backendRefs:
        - name: zigbee
          port: 80
