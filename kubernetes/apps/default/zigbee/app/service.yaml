apiVersion: v1
kind: Service
metadata:
  name: zigbee
  namespace: default
  labels:
    app.kubernetes.io/name: zigbee
    app.kubernetes.io/instance: zigbee
    app.kubernetes.io/controller: zigbee
    app.kubernetes.io/service: zigbee
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app.kubernetes.io/name: zigbee
    app.kubernetes.io/instance: zigbee
    app.kubernetes.io/controller: zigbee
