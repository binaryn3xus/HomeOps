apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sabnzbd
  namespace: default
  annotations:
    gatus.home-operations.com/endpoint: 'dns: { query-name: sabnzbd.unscfleet.com }'
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - sabnzbd.unscfleet.com
  rules:
    - backendRefs:
        - name: sabnzbd
          port: 80
