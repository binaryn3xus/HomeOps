---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app teleport
  namespace: default
spec:
  interval: 30m
  chart:
    spec:
      chart: teleport-cluster
      version: 11.3.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: teleport
        namespace: flux-system

  values:
    clusterName: teleport.${SECRET_DOMAIN}
    #kubeClusterName: cluster-0

    authentication:
      type: github

    tls:
      existingSecretName: teleport-tls

    # annotations:
    #   service:
    #     coredns.io/hostname: teleport.${SECRET_DOMAIN}

    persistence:
      enabled: true
      existingClaimName: config-teleport

    service:
      spec:
        loadBalancerIP: ${SVC_TELEPORT_ADDR}
