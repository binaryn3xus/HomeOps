---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: node-red
  namespace: default
spec:
  data:
    - remoteRef:
        key: SSH-NodeRed-Private-Key
      secretKey: NODERED-SSH-PRIVATE-KEY
    - remoteRef:
        key: SSH-NodeRed-Public-Key
      secretKey: NODERED-SSH-PUBLIC-KEY
  refreshInterval: "12h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault
  target:
    creationPolicy: Owner
    name: node-red-secret
    template:
      data:
        NODERED-SSH-PRIVATE-KEY: "{{ .NODERED-SSH-PRIVATE-KEY }}"
        NODERED-SSH-PUBLIC-KEY: "{{ .NODERED-SSH-PUBLIC-KEY }}"
      engineVersion: v2
