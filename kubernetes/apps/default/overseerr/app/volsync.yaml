---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: overseerr-restic
  namespace: default
spec:
  secretStoreRef:
    name: azure-store
    kind: ClusterSecretStore
  refreshInterval: "12h"
  target:
    deletionPolicy: Delete
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        RESTIC_REPOSITORY: "{{ .ResticRepositoryBase }}/overseerr"
        RESTIC_PASSWORD: "{{ .ResticPassword }}"
        AWS_ACCESS_KEY_ID: "{{ .AWSAccessKeyId }}"
        AWS_SECRET_ACCESS_KEY: "{{ .AWSSecretAccessKey }}"
  data:
    - secretKey: ResticRepositoryBase
      remoteRef:
        key: Restic-Repository-Base
    - secretKey: ResticPassword
      remoteRef:
        key: Restic-Password
    - secretKey: AWSAccessKeyId
      remoteRef:
        key: AWS-Acess-Key-Id
    - secretKey: AWSSecretAccessKey
      remoteRef:
        key: AWS-Secret-Access-Key
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: overseerr
  namespace: default
spec:
  sourcePVC: config-overseerr-0
  trigger:
    schedule: "0 4 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 10
    repository: overseerr-restic
    cacheCapacity: 10Gi
    volumeSnapshotClassName: csi-ceph-blockpool
    storageClassName: ceph-block
    moverSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    retain:
      daily: 10
      within: 3d
