---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/resources.teleport.dev/teleportrolev8_v1.json
apiVersion: resources.teleport.dev/v1
kind: TeleportRoleV8
metadata:
  name: desktop-user
  namespace: teleport
spec:
  allow:
    windows_desktop_logins: ['{{internal.logins}}', 'joshua', 'jimmy']
    windows_desktop_labels:
      "*": "*"
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/resources.teleport.dev/teleportrolev8_v1.json
apiVersion: resources.teleport.dev/v1
kind: TeleportRoleV8
metadata:
  name: cluster-admin
  namespace: teleport
spec:
  allow:
    logins: ['{{internal.logins}}', 'root', 'joshua', 'jimmy']
    node_labels:
      "*": "*"
    windows_desktop_labels:
      "*": "*"
    windows_desktop_logins: ['joshua', 'jimmy']
    rules:
      - resources: ["*"]
        verbs: ["*"]
