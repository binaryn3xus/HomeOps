---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: teleport-internal-tls
  namespace: teleport
spec:
  secretName: teleport-internal-tls-secret
  issuerRef:
    name: internal-ca-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - "teleport.teleport.svc.cluster.local"
    - "teleport.teleport.svc"
