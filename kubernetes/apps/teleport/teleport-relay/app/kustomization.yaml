---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: teleport
resources:
  - helmrelease.yaml
  #- ../../../../templates/volsync
configMapGenerator:
  - name: teleport-relay-configmap
    files:
      - teleport.yaml=./config/teleport.yaml
commonLabels:
  app.kubernetes.io/name: teleport-relay
  app.kubernetes.io/instance: teleport-relay
