---
# yaml-language-server: $schema=https://raw.githubusercontent.com/openclaw-rocks/openclaw-operator/main/config/crd/bases/openclaw.rocks_openclawinstances.yaml
apiVersion: openclaw.rocks/v1alpha1
kind: OpenClawInstance
metadata:
  name: cortana
  namespace: ai
spec:
  selfConfigure:
    enabled: true
  security:
    networkPolicy:
      enabled: true
      allowedIngressNamespaces:
        - "network"
    rbac:
      createServiceAccount: true
  envFrom:
    - secretRef:
        name: openclaw-cortana-secret
  env:
    - name: PATH
      value: "/home/openclaw/.openclaw/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  storage:
    persistence:
      enabled: true
      existingClaim: openclaw-cortana-data
  config:
    mergeMode: "merge"
    raw:
      gateway:
        port: 18789
        controlUi:
          allowedOrigins:
            - "https://openclaw.unscfleet.com"
          dangerouslyDisableDeviceAuth: false
