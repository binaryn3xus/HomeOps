apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app kokoro-fastapi-gpu
  namespace: ai
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: *app
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      kokoro:
        containers:
          app:
            image:
              repository: ghcr.io/remsky/kokoro-fastapi-gpu
              tag: latest
            env:
              TZ: America/New_York
            resources:
              requests:
                cpu: "500m"
                memory: 1Gi
              limits:
                memory: 1.5Gi
                nvidia.com/gpu: 1
    defaultPodOptions:
      nodeSelector:
        nvidia.com/gpu.present: "true"
      tolerations:
        - key: sku
          operator: Equal
          value: gpu
          effect: NoSchedule
      runtimeClassName: nvidia
    service:
      app:
        controller: kokoro
        ports:
          http:
            port: 8000
            targetPort: 8880
