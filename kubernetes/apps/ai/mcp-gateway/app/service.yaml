apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway-svc
  namespace: ai
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 9000
      protocol: TCP
      name: http
  selector:
    app: mcp-gateway
