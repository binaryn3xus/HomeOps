---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: home-ops
  namespace: argocd
spec:
  description: Project for HomeOps applications
  # Allow deployment to any namespace in the local cluster
  destinations:
    - server: https://kubernetes.default.svc
      namespace: '*'
  # Allow resources from your repo AND the common Helm/OCI mirrors
  sourceRepos:
    - https://github.com/binaryn3xus/HomeOps/
    - oci://ghcr.io/*
    - oci://quay.io/*
    - https://charts.fairwinds.com/stable
    - https://kubernetes-sigs.github.io/node-feature-discovery/charts
    - https://nvidia.github.io/k8s-device-plugin
    - https://prometheus-community.github.io/helm-charts
    - https://grafana.github.io/helm-charts
  # Allow all resource types
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
  # Sync Windows configuration
  orphanedResources:
    warn: true
