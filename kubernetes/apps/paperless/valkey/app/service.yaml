apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: paperless
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app.kubernetes.io/name: valkey
    app.kubernetes.io/instance: valkey
    app.kubernetes.io/controller: valkey
