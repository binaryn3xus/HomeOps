apiVersion: v1
kind: Service
metadata:
  name: palworld-server-dashboard
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: palworld-server-dashboard
    app.kubernetes.io/instance: palworld-server-dashboard
    app.kubernetes.io/controller: palworld-server-dashboard
