apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
  annotations:
    gatus.home-operations.com/endpoint: |-
      group: internal
      dns:
        query-name: sonarr.unscfleet.com
        query-type: A
      url: 1.1.1.1
      conditions:
        - len([BODY]) == 0
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - sonarr.unscfleet.com
  rules:
    - backendRefs:
        - name: sonarr
          port: 80
