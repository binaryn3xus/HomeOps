apiVersion: v1
kind: Service
metadata:
  name: seerr
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app.kubernetes.io/name: seerr
    app.kubernetes.io/instance: seerr
    app.kubernetes.io/controller: seerr
