apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: qbittorrent
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - qbittorrent.unscfleet.com
  rules:
    - backendRefs:
        - name: qbittorrent
          port: 80
