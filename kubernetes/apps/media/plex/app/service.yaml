apiVersion: v1
kind: Service
metadata:
  name: plex
  annotations:
    lbipam.cilium.io/ips: "10.0.30.34"
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 32400
      targetPort: 32400
      protocol: TCP

