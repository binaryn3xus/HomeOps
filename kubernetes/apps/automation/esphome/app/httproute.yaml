apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: esphome
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - esphome.unscfleet.com
  rules:
    - backendRefs:
        - name: esphome
          port: 6052
