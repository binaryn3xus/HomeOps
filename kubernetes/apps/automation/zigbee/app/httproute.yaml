apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zigbee
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - zigbee.unscfleet.com
  rules:
    - backendRefs:
        - name: zigbee
          port: 80
