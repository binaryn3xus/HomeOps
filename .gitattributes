# Automatically normalize line endings to LF
* text=auto eol=lf

# Treat .yaml.j2 files as YAML for syntax highlighting
*.yaml.j2 linguist-language=YAML

# Use sopsdiffer for diffing encrypted files
*.sops.* diff=sopsdiffer